POSTGRES_DB=job_platform
POSTGRES_USER=job_user
POSTGRES_PASSWORD=job_password

# Shared secret for POST /jobs/callback (header X-Callback-Secret). Leave empty only for local dev.
CALLBACK_SECRET=change-me-before-production
